version: '3.7'

services:
  #my-database: 컴퓨터이름

  # my-database:
  #   build:
  #     context: ./services/resource
  #     dockerfile: Dockerfile.mysql
  #   environment:
  #     MYSQL_DATABASE: 'mydocker' #env 파일과 동일해야함
  #     MYSQL_ROOT_PASSWORD: '12345678' #env 파일과 동일해야함
  #   ports:
  #     - 3306:3306

  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    volumes:
      - ./api-gateway/src:/api-gateway/src
    ports:
      - 3000:3000

  auth-service:
    build:
      context: ./services/auth
      dockerfile: Dockerfile
    volumes:
      - ./services/auth/src:/auth-service/src

  resource-service:
    build:
      context: ./services/resource
      dockerfile: Dockerfile
    volumes:
      - ./services/resource/src:/resource-service/src
